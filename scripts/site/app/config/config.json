{
    "name":       "EXAMPLE_APP",


    //
    // Paths we should check for the autoloaders.
    // If this is an object, the keys represent the environment.
    //   [.php] files   - executed as PHP scripts
    //
    "autoload":   {
        "_":   [ "CONFIG_PATH/autoload/autoload.php" ],
        "dev": [ "CONFIG_PATH/autoload/autoload.php" ]
    },

    //
    // Information about the controllers we're going to use
    //
    "controller": {
        "namespace": "\\EXAMPLE_APP_NAMESPACE\\Controller\\"
    },


    //
    // These are the paths referenced by the application/framework
    //
    "paths":      {
        "views":   "APP_PATH/VIEW/",
        "models":  [
            "CONFIG_PATH/models/models.json"
        ],
        "routes":  [
            "CONFIG_PATH/routes/routes.json",
            "CONFIG_PATH/routes/routes.php"
        ],
        "sources": {
            "_": [
                "CONFIG_PATH/sources/sources.json"
            ]
        }
    }
}